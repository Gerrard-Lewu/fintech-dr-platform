apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auditor-monitor
  labels:
    release: monitoring-stack
spec:
  selector:
    matchLabels:
      app: auditor
  namespaceSelector:
    matchNames:
      - default
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s